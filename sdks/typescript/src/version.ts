// VERSION is the SDK version, reported in the default User-Agent header.
// Keep in sync with package.json; the release workflow stamps the package
// version from the git tag at publish time.
export const VERSION = "0.0.1";
