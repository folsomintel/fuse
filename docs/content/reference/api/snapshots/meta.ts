import { defineMeta } from "blume";

// the pages under this folder are generated from the openapi spec; this meta
// only gives the tag's sidebar group a title and icon.
export default defineMeta({
  title: "Snapshots",
  icon: "camera",
});
