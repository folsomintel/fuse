import { defineMeta } from "blume";

export default defineMeta({
  title: "Environments",
  icon: "container",
  pages: ["index", "list", "get", "create", "destroy", "drain", "fork", "rotate-token", "watch", "exec", "shell"],
});
