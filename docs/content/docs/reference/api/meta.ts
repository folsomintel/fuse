import { defineMeta } from "blume";

export default defineMeta({
  title: "HTTP API",
  icon: "webhook",
  pages: ["index", "environments", "snapshots", "hosts", "api-keys", "auth-endpoints", "health"],
});
