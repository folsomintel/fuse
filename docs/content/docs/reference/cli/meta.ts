import { defineMeta } from "blume";

export default defineMeta({
  title: "CLI",
  icon: "terminal",
  pages: ["index", "contexts", "hosts", "environment", "up", "init", "snapshot", "apikeys", "metrics"],
});
