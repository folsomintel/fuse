---
title: Hosts
description: Registering and managing the compute hosts the scheduler places VMs onto
icon: HardDrive
---

`client.Hosts` registers and manages the compute hosts the scheduler places
microVMs onto. Register a host, letting the orchestrator probe its real
hardware capacity rather than trusting a guess:

```go
host, err := client.Hosts.Register(ctx, fuse.RegisterHostRequest{
	ID:      "prod-east-1",
	URL:     "http://10.0.0.5:9000",
	Token:   agentToken,
	Region:  "us-east",
	Backend: "firecracker",
	Capacity: fuse.HostCapacity{
		VMCount: 20, // required; cpus/ram_mb/storage_gb left at 0 to probe
	},
})
if err != nil {
	log.Fatal(err)
}
for _, w := range host.Warnings {
	log.Println("warning:", w)
}
```

- `ID` is a free-form, operator-supplied string and doubles as the host's
  primary key. `Backend` defaults to `"firecracker"` if left empty; only
  `"qemu"` hosts may register with `Capacity.GPUs > 0`.
- `Capacity.CPUs`, `RamMB`, and `StorageGB` are optional hardware facts:
  leaving any at `0` (as above) has the orchestrator probe the host agent for
  the real value instead of trusting a guess. Pass one explicitly to override
  the probe, for a deliberate overcommit or carve-out, a declared value above
  what the probe reports still registers, with a warning rather than a
  rejection. If the host's provider doesn't support probing and a value was
  left at `0`, registration fails with a `502` `*APIError` instead, declare
  it explicitly in that case.
- `Capacity.VMCount` is scheduling policy, not something a host can report on
  its own, so it's never probed and is always required.
- `host.Warnings` is only ever populated on this call's response, non-fatal
  notices like a declared value exceeding what was probed.

## Everything else

| Method | Purpose |
| --- | --- |
| `List(ctx)` | List every registered host with its declared `Capacity` and live `Allocated` figures. |
| `Get(ctx, hostID)` | Fetch one host by ID. |
| `Cordon(ctx, hostID)` / `Uncordon(ctx, hostID)` | Stop or resume scheduling new environments onto the host, without touching what's already running. |
| `Deregister(ctx, hostID)` | Remove the host. Fails with a `conflict` `*APIError` if environments are still assigned to it. |

`Host.State` is one of `active`, `cordoned`, or `draining`. `Host.LastSeen` is
set once at registration and never refreshed, it is not a liveness signal.
These mirror the [HTTP API](/docs/reference/api/hosts) one to one.
