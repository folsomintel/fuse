---
title: API keys
description: Issuing and revoking orchestrator-global API keys
icon: KeySquare
---

`client.APIKeys` mints and revokes API keys. All three methods require the
master token, the server enforces this regardless of what token the client
was constructed with.

```go
key, err := client.APIKeys.Create(ctx, "ci-runner")
if err != nil {
	log.Fatal(err)
}
fmt.Println("save this now:", key.Key) // shown exactly once
```

`label` is optional. `key.Key` holds the raw secret and is returned exactly
once, it cannot be recovered afterward, store it immediately.

## Everything else

| Method | Purpose |
| --- | --- |
| `List(ctx)` | List key metadata, `APIKey` has no `Key` field. `LastUsedAt`/`RevokedAt` are `nil` until they happen. |
| `Revoke(ctx, id)` | Revoke the key with the given `id` (the `APIKey.ID`, not the secret itself). |

These mirror the [HTTP API](/docs/reference/api/api-keys) one to one.
