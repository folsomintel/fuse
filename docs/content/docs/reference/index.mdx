---
title: Overview
description: SDKs, CLI, and HTTP API reference for driving a Fuse orchestrator
icon: BookOpen
---

import { Terminal, Braces, Webhook, KeySquare } from 'lucide-react';

This section is the reference for driving a running Fuse orchestrator: the three
client SDKs, the operator CLI, and the raw HTTP API underneath both. If you're new
to Fuse's concepts, start with [Learn](/docs/learn) first. This section assumes you
already know what an environment, host, and snapshot are.

## Picking a surface

<Cards>
  <Card
    icon={<Terminal />}
    title="CLI"
    description="The fastest way to operate Fuse by hand: register hosts, bring up environments, inspect and tear things down."
    href="/docs/reference/cli"
  />
  <Card
    icon={<Braces />}
    title="SDKs"
    description="Drive Fuse programmatically from your own service or agent harness. Go, Python, and TypeScript, all mirroring the same shape."
    href="/docs/reference/sdks/go"
  />
  <Card
    icon={<Webhook />}
    title="HTTP API"
    description="The raw REST surface every SDK and the CLI itself are built on."
    href="/docs/reference/api"
  />
  <Card
    icon={<KeySquare />}
    title="Authentication"
    description="How the orchestrator authenticates callers, across the CLI, SDKs, and browser."
    href="/docs/reference/auth"
  />
</Cards>

## Resource coverage

Every surface exposes the same four resources:

| Resource | What it manages |
| --- | --- |
| Environments | Create, list, inspect, drain, fork, destroy microVMs. |
| Hosts | Register, list, cordon/uncordon, remove compute hosts. |
| Snapshots | Create, list, restore, delete VM snapshots. |
| API keys | Issue and revoke orchestrator-global API keys (master-token only). |
