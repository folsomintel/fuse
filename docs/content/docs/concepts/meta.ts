import { defineMeta } from "blume";

export default defineMeta({
  title: "Concepts",
  icon: "layers",
  pages: ["fusefile", "providers", "hosts", "environments", "snapshots", "artifacts", "scheduling", "reconcile", "state-and-recovery"],
});
