{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/folsomintel/fuse/main/schema/fusefile-v1.json",
  "title": "Fusefile v1",
  "description": "The authoring format for a Fuse environment. Advisory only: the CLI's Parse and Compile are authoritative, and where this schema and Go disagree, Go is right and this schema is the bug.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version"],
  "$comment": "build and setup are the same field under two names, so setting both is rejected.",
  "not": { "required": ["build", "setup"] },
  "properties": {
    "version": {
      "const": 1,
      "description": "Fusefile format version. Always 1."
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?$",
      "description": "Task id this Fusefile boots under, and through it the environment's name: the orchestrator prefixes it, so `name: sandbox` is the environment `fuse-sandbox`. Optional; `fuse up --task-id` overrides it, and with neither the CLI falls back to the parent directory's name. A DNS label: lowercase letters, digits and dashes, alphanumeric at both ends, 63 chars max."
    },
    "image": {
      "type": "string",
      "description": "Name of a rootfs pre-baked on the host, not an OCI reference. Omit for the host's default base rootfs."
    },
    "resources": { "$ref": "#/$defs/resources" },
    "placement": { "$ref": "#/$defs/placement" },
    "cache": { "$ref": "#/$defs/cache" },
    "files": {
      "type": "array",
      "description": "Files materialized in the guest before the build steps run. Total content is capped at 64KiB.",
      "items": { "$ref": "#/$defs/file" }
    },
    "copy": {
      "type": "array",
      "description": "Local files and directories shipped into the guest before the build steps run. Uploaded alongside the create request rather than embedded in the startup script, so the combined size is capped at 512KiB. Permissions are not preserved: chmod in build.",
      "items": { "$ref": "#/$defs/copyEntry" }
    },
    "env": {
      "type": "object",
      "description": "Environment variables set for every build step and for run. Not inherited by services, which carry their own env. A name must be a shell identifier. A secret-backed value is resolved by the orchestrator into a file under /fuse that the startup script sources, so it never appears in the script text.",
      "propertyNames": { "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
      "additionalProperties": { "$ref": "#/$defs/envValue" }
    },
    "build": {
      "type": "array",
      "description": "Steps that prepare the environment. They run to completion, in the workspace, before run.",
      "items": { "$ref": "#/$defs/step" }
    },
    "setup": {
      "type": "array",
      "deprecated": true,
      "description": "Deprecated alias for build, kept accepted so existing Fusefiles keep working. Setting both build and setup is an error.",
      "items": { "$ref": "#/$defs/step" }
    },
    "services": {
      "type": "object",
      "description": "In-VM services, keyed by service name. Compiles to manifest.services and a compose project.",
      "additionalProperties": { "$ref": "#/$defs/service" }
    },
    "run": {
      "description": "The main entrypoint, compile into `startup_script` after `build`. A string is interpreted by `sh -lc` (shell form); a list is an argv, each element shell-quoted into a single command line so no argument is subject to word splitting, globbing, or metacharacter reinterpretation (exec form). The exec form is not a true execve: the generated command still runs inside `sh -lc`. The shell form compiles to a byte-identical script; only the exec form adds per-argument quoting.",
      "oneOf": [
        { "type": "string", "minLength": 1 },
        {
          "type": "array",
          "description": "Exec form argv: each element is a single argument, shell-quoted as a unit.",
          "items": { "type": "string" },
          "minItems": 1
        }
      ]
    },
    "workspace": {
      "type": "string",
      "description": "Working directory inside the guest, created with mkdir -p; the build steps and run execute there. Must be an absolute path with no '..' segments. Defaults to /workspace.",
      "pattern": "^/",
      "not": { "pattern": "(^|/)\\.\\.(/|$)" }
    },
    "expose": {
      "type": "array",
      "description": "Guest ports published to the outside world.",
      "items": { "$ref": "#/$defs/expose" }
    },
    "secrets": {
      "type": "array",
      "description": "Secret names this environment requires. Values are supplied out of band, never written here.",
      "items": { "type": "string" }
    },
    "healthcheck": { "$ref": "#/$defs/envHealthcheck" },
    "desktop": { "$ref": "#/$defs/desktop" },
    "startup_timeout": {
      "allOf": [{ "$ref": "#/$defs/duration" }],
      "description": "Go duration bounding build + run. Omit for the orchestrator default (30s)."
    }
  },
  "$defs": {
    "duration": {
      "type": "string",
      "description": "A go duration, e.g. \"45s\", \"1h30m\", \"100ms\".",
      "pattern": "^(0|([0-9]*\\.?[0-9]+(ns|us|ms|s|m|h))+)$"
    },
    "size": {
      "type": "string",
      "description": "A number, optional whitespace, and a unit: M, MB, MiB, G, GB, GiB, T, TB or TiB, in any case. Every unit is binary, so GB and GiB are synonyms for 1024 MiB. E.g. \"512MB\", \"2GB\", \"2 GiB\", \"1.5GB\".",
      "pattern": "^[0-9]+(\\.[0-9]+)?[ \\t]*([MmGgTt]|[MmGgTt][Bb]|[MmGgTt][Ii][Bb])$"
    },
    "label": {
      "type": "string",
      "description": "A placement label key or value: alphanumeric at both ends, dots, dashes and underscores inside, 63 characters max.",
      "pattern": "^[a-zA-Z0-9]([-._a-zA-Z0-9]{0,61}[a-zA-Z0-9])?$"
    },
    "resources": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cpus": {
          "type": "number",
          "multipleOf": 1,
          "minimum": 0,
          "description": "Whole vCPU count. A whole-valued float (2.0) is accepted; a fraction is not, because Firecracker takes an integer vcpu_count and QEMU an integer -smp."
        },
        "gpu": {
          "type": "integer",
          "minimum": 0,
          "description": "Device count: whole GPUs, or MIG instances when gpu_profile is set."
        },
        "gpu_kind": {
          "type": "string",
          "description": "Optional GPU model match, e.g. \"a100\". Empty matches any kind."
        },
        "gpu_profile": {
          "type": "string",
          "description": "Optional MIG profile in nvidia mig-parted vocabulary, e.g. \"1g.10gb\". When set, gpu counts MIG instances and must be at least 1.",
          "pattern": "^[1-7][Gg]\\.[0-9]+[Gg][Bb](\\+[Mm][Ee])?$"
        },
        "memory": { "$ref": "#/$defs/size" },
        "disk": {
          "allOf": [{ "$ref": "#/$defs/size" }],
          "description": "Size of the guest root disk, rounded up to a whole GB. The preferred spelling; storage is a permanent alias."
        },
        "storage": {
          "allOf": [{ "$ref": "#/$defs/size" }],
          "description": "A permanent alias for disk. Setting both is only valid if they name the same size."
        },
        "region": {
          "type": "string",
          "description": "Schedules only onto a host registered in this region. Empty matches any region."
        },
        "max_runtime": {
          "allOf": [{ "$ref": "#/$defs/duration" }],
          "description": "Hard ceiling measured from create. A leak-detection ceiling, not a task budget."
        },
        "idle_timeout": {
          "allOf": [{ "$ref": "#/$defs/duration" }],
          "description": "Destroys the environment after this long with no exec and no attach session. Minimum 1m."
        }
      }
    },
    "placement": {
      "type": "object",
      "description": "Hard placement constraints. Every field is a gate, not a preference.",
      "additionalProperties": false,
      "properties": {
        "host": {
          "type": "string",
          "description": "Pins the environment to an exact host id."
        },
        "labels": {
          "type": "object",
          "description": "Label selectors matched against the host's declared labels. Every pair must match.",
          "propertyNames": { "$ref": "#/$defs/label" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        }
      }
    },
    "cache": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Opt in to the build layer cache. Off by default."
        }
      }
    },
    "file": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path"],
      "oneOf": [{ "required": ["source"] }, { "required": ["content"] }],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Where the file lands in the guest. A relative path resolves against the workspace."
        },
        "source": {
          "type": "string",
          "description": "A path on the authoring machine, relative to the Fusefile."
        },
        "content": {
          "type": "string",
          "description": "A literal file body."
        },
        "mode": {
          "type": "string",
          "description": "Octal permission string, e.g. \"0755\".",
          "pattern": "^0?[0-7]{3}$"
        }
      }
    },
    "copyEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to"],
      "properties": {
        "from": {
          "type": "string",
          "minLength": 1,
          "description": "A file or directory on the authoring machine, relative to the Fusefile. A directory is walked into one upload per regular file; a symlink is an error."
        },
        "to": {
          "type": "string",
          "minLength": 1,
          "description": "Where it lands in the guest. A relative path resolves against the workspace. No '..' segments, and nothing under /fuse, which belongs to the guest agent.",
          "not": { "pattern": "(^|/)\\.\\.(/|$)" }
        }
      }
    },
    "step": {
      "description": "One build step: either a bare command string or a mapping.",
      "oneOf": [
        { "type": "string" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["run"],
          "properties": {
            "run": {
              "type": "string",
              "minLength": 1,
              "description": "The step's shell command."
            },
            "inputs": {
              "type": "array",
              "description": "Files whose contents are hashed into the step's cache key. Relative to the Fusefile's directory.",
              "items": { "type": "string", "minLength": 1 }
            },
            "cache": {
              "type": "boolean",
              "description": "Set false to make the step uncacheable. Required for a step that reads secrets or has effects outside the rootfs."
            },
            "workdir": {
              "type": "string",
              "minLength": 1,
              "description": "Directory this one step runs in. A relative path resolves against workspace. The step is wrapped in a subshell, so the directory does not leak into the next step. No '..' segments.",
              "not": { "pattern": "(^|/)\\.\\.(/|$)" }
            }
          }
        }
      ]
    },
    "service": {
      "type": "object",
      "additionalProperties": false,
      "required": ["image"],
      "properties": {
        "image": {
          "type": "string",
          "minLength": 1,
          "description": "An OCI image reference. Unlike the top-level image, this one is pulled."
        },
        "ports": {
          "type": "array",
          "items": { "type": "integer" }
        },
        "env": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/envValue" }
        },
        "command": {
          "type": "array",
          "items": { "type": "string" }
        },
        "restart": {
          "type": "string",
          "description": "Compose-native restart policy.",
          "enum": ["no", "always", "on-failure", "unless-stopped"]
        },
        "healthcheck": { "$ref": "#/$defs/healthcheck" },
        "depends_on": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "healthcheck": {
      "type": "object",
      "description": "A compose-native container healthcheck. It governs the container, not the VM.",
      "additionalProperties": false,
      "properties": {
        "test": {
          "type": "array",
          "items": { "type": "string" }
        },
        "interval": { "$ref": "#/$defs/duration" },
        "timeout": { "$ref": "#/$defs/duration" },
        "retries": { "type": "integer" }
      }
    },
    "envHealthcheck": {
      "type": "object",
      "description": "The environment-level readiness probe: one verdict for whether the whole sandbox is doing its job, reported back to the control plane. Not the same thing as services.*.healthcheck, which governs one container and never reaches the orchestrator.",
      "additionalProperties": false,
      "oneOf": [{ "required": ["http"] }, { "required": ["exec"] }],
      "properties": {
        "http": {
          "type": "object",
          "description": "An HTTP GET inside the guest. A 2xx or 3xx response passes.",
          "additionalProperties": false,
          "required": ["port"],
          "properties": {
            "port": {
              "type": "integer",
              "minimum": 1,
              "maximum": 65535,
              "description": "The guest port to dial. It needs no expose entry: the probe runs inside the guest."
            },
            "path": {
              "type": "string",
              "description": "Request path, which must start with a slash. Defaults to /.",
              "pattern": "^/"
            }
          }
        },
        "exec": {
          "type": "array",
          "description": "An argv run inside the guest. Exit status 0 passes.",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        },
        "interval": {
          "allOf": [{ "$ref": "#/$defs/duration" }],
          "description": "How long the guest agent waits between attempts. Omit for the guest agent default (10s)."
        },
        "timeout": {
          "allOf": [{ "$ref": "#/$defs/duration" }],
          "description": "Bound on one attempt. Must not exceed interval. Omit for the guest agent default (2s)."
        },
        "retries": {
          "type": "integer",
          "minimum": 0,
          "description": "Consecutive failures that flip the verdict to failing. Omit or 0 for the guest agent default (3)."
        },
        "start_period": {
          "allOf": [{ "$ref": "#/$defs/duration" }],
          "description": "Grace window from the first attempt during which failures are not counted. Ends for good once the probe passes once."
        }
      }
    },
    "desktop": {
      "type": "object",
      "description": "The geometry of the environment's graphical session. Requires an image that carries the desktop stack; on any other image the declaration is inert. The display number is fixed at :1.",
      "additionalProperties": false,
      "required": ["width", "height"],
      "properties": {
        "width": {
          "type": "integer",
          "minimum": 320,
          "maximum": 3840,
          "description": "Display width in pixels."
        },
        "height": {
          "type": "integer",
          "minimum": 320,
          "maximum": 3840,
          "description": "Display height in pixels."
        }
      }
    },
    "envValue": {
      "type": "object",
      "description": "Either a literal value or a secret reference. Exactly one is set.",
      "additionalProperties": false,
      "oneOf": [{ "required": ["value"] }, { "required": ["secret"] }],
      "properties": {
        "value": { "type": "string" },
        "secret": {
          "type": "string",
          "description": "Name of a secret supplied at up time."
        }
      }
    },
    "expose": {
      "type": "object",
      "additionalProperties": false,
      "required": ["port"],
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "description": "The guest port to publish. Reserved guest control-plane ports (the agent's management port 9550 and SSH 22) are always rejected. Privileged ports below 1024 are rejected by default; set allow_reserved to true to publish one."
        },
        "as": {
          "type": "string",
          "description": "An optional name for the published endpoint."
        },
        "allow_reserved": {
          "type": "boolean",
          "default": false,
          "description": "Opt in to exposing a privileged guest port (below 1024) that is otherwise blocked by default. The guest agent's management port (9550, FUSED_PORT) and SSH (22) are always blocked and cannot be opted in to."
        }
      }
    }
  }
}
