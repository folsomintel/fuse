# Secrets / local env — never commit
.env
.env.*
!.env.example

# Build output
/bin/
*.test
coverage/
*.out

# Stray binaries from `go build ./cmd/...` at the repo root
/fused
/orchestrator
/dbcheck

# OS / editor
.DS_Store

# GoReleaser build output
dist/

.claude

# docs site build/dep output (docs/ itself is tracked)
docs/node_modules/
docs/.next/
docs/.source/